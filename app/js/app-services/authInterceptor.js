// myApp.factory('AuthInterceptor', [function() {
//     return {
// 		// Sent the token (if present) with each request
//         'request': function(config) {
// 			config.headers = config.headers || {};
// 			let auth = btoa("bill:abc123");
// 			config.headers.Authorization = 'Basic ' + auth;
//            return config;
//         }
//     };
// }]);

